HW requirements:
NVIDIA GPU, preferably TitanX

SW requirements:
Keras + Tensorflow
Anaconda
pandas
tqdm

code structure:

root
    udacity_capstone
        ipython notebooks that presents data exploration, benchmark model, sequential model training and visualization
        in order to run ensemble_best_models.ipynb first dumps have been created from pretrained models checkpoints.
    All other code is in the root directory.
    To run training or resnet need to run train.py with the arguments according to help
